# Конфигурация для мини-приложения уроков
server {
    listen 80;
    server_name localhost yourdomain.com www.yourdomain.com;
    
    # Перенаправление на HTTPS (для продакшena)
    # return 301 https://$server_name$request_uri;
    
    location / {
        proxy_pass http://app:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }
    
    # Статические файлы
    location /_next/static/ {
        proxy_pass http://app:3000;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
    
    # Загружаемые файлы
    location /uploads/ {
        proxy_pass http://app:3000;
        add_header Cache-Control "public, max-age=86400";
    }
    
    # Фавикон
    location /favicon.ico {
        proxy_pass http://app:3000;
        add_header Cache-Control "public, max-age=86400";
    }
    
    # Проверка здоровья
    location /health {
        proxy_pass http://app:3000/api/health;
    }
}

# HTTPS конфигурация (раскомментируйте для продакшena с SSL)
# server {
#     listen 443 ssl http2;
#     server_name yourdomain.com www.yourdomain.com;
#     
#     ssl_certificate /etc/ssl/certs/lessons.crt;
#     ssl_certificate_key /etc/ssl/certs/lessons.key;
#     
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-SHA384;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     
#     location / {
#         proxy_pass http://app:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_read_timeout 86400;
#     }
# }